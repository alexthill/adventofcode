This year I try to micro-optimize all solutions to run as fast as possible (without using unsafe code).

Run a particular day with `cargo run --release -- [day]` or all days with `cargo run --release -- all`.
